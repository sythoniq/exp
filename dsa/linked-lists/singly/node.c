#include "node.h"

void initList(ll_node* node) {
  // Declare root node only.
  
  node->head = NULL;
  node->tail = NULL;
  node->data = 0;
}

void addToList(char* point, ll_node* node, int index) {
   
}
